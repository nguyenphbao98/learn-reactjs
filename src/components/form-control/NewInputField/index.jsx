import React from 'react';
import PropTypes from 'prop-types';

NewInputField.propTypes = {
    
};

function NewInputField(props) {
    return (
        <div>
            
        </div>
    );
}

export default NewInputField;